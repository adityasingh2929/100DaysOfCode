// Treats all JS Code as newer js version, although it does it now anyways but writing this is considered a good practice.
"use strict"; 


// this throws an error, so use the terminator, though if they're both in seperate lines, then no error, but using terminator is considered a good practice.
// console.log(3+3) console.log(2*5)


// When JS was created, there was a standard created that was to follow by everyone (it defined js rules nd syntax),
// This was Ecma standard.


// Symbol datatype will be taught when we'll learn react
// SYMBOL -> unique.